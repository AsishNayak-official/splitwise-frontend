import React from 'react'
import { Button } from '../ui/button'

const Transactions = () => {
  return (
        <Button variant="outline" className="rounded-full">Transaction History</Button>
  )
}

export default Transactions